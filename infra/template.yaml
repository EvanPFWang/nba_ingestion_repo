AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >-
  Example SAM/CloudFormation template for the NBA Data Lakehouse.  This
  template provisions S3 buckets, IAM roles, a Glue database, a
  Step Functions state machine, ECS cluster, and an EventBridge Scheduler rule for
  orchestrating the DataUpdater ingestion.

Parameters:
  BronzeBucketName:
    Type: String
    Description: S3 bucket for raw (Bronze) data
    Default: nba-lake-bronze
  SilverBucketName:
    Type: String
    Description: S3 bucket for Silver data (Iceberg)
    Default: nba-lake-silver
  GoldBucketName:
    Type: String
    Description: S3 bucket for Gold data (BI-ready)
    Default: nba-lake-gold
  DataUpdaterImage:
    Type: String
    Description: ECR image URI for DataUpdater container
  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: ID of the VPC where the ECS task will run
  SubnetIds:
    Type: List<AWS::EC2::Subnet::Id>
    Description: Subnets for Fargate task (private subnets recommended)
  InitialStartDate:
    Type: String
    Default: "2015-10-01"
    Description: Start date for initial backfill
  InitialIngestionDate:
    Type: String
    Default: ""
    Description: End date for initial backfill (default=today)
Resources:
  ### S3 Buckets ###
  BronzeBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref BronzeBucketName
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      LifecycleConfiguration:
        Rules:
          - Id: TransitionToIA
            Status: Enabled
            Transitions:
              - StorageClass: STANDARD_IA
                TransitionInDays: 90

  SilverBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref SilverBucketName
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256

  GoldBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref GoldBucketName
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256

  #secrets manager for bball-index creds
  BballCredentialsSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: nba-ingestion/bball-index
      Description: Bball-Index login credentials
      SecretString: '{"email":"","password":""}'

  ### Glue Database and Example Iceberg Table ###
  NbaDatabase:
    Type: AWS::Glue::Database
    Properties:
      CatalogId: !Ref AWS::AccountId
      DatabaseInput:
        Name: nba
        Description: NBA Silver and Gold tables (Iceberg)


  BoxScoresTable:
    Type: AWS::Glue::Table
    Properties:
      CatalogId: !Ref AWS::AccountId
      DatabaseName: !Ref NbaDatabase
      TableInput:
        Name: box_scores
        TableType: EXTERNAL_TABLE
        Parameters:
          classification: iceberg
          format: parquet
          write.target.file.size.bytes: '536870912'
        StorageDescriptor:
          Columns:
            - Name: game_id
              Type: string
            - Name: player_id
              Type: string
            - Name: points
              Type: int
            # Add additional columns as needed
          Location: !Sub 's3://${SilverBucketName}/silver/nba/box_scores/'
          InputFormat: org.apache.iceberg.mr.hive.HiveIcebergInputFormat
          OutputFormat: org.apache.iceberg.mr.hive.HiveIcebergOutputFormat
          SerdeInfo:
            SerializationLibrary: org.apache.iceberg.mr.hive.HiveIcebergSerDe
            Parameters: {}
      PartitionKeys:
        - Name: season
          Type: string

  ### IAM Role for ECS Task ###
  TaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: nba-data-updater
      Cpu: '512'
      Memory: '1024'
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      ExecutionRoleArn: !GetAtt TaskExecutionRole.Arn
      TaskRoleArn: !GetAtt TaskRole.Arn
      ContainerDefinitions:
        - Name: updater
          Image: !Ref DataUpdaterImage
          Essential: true
          Environment:
            - Name: BRONZE_BUCKET
              Value: !Ref BronzeBucketName
            - Name: BRONZE_PREFIX
              Value: bronze/nba
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            - Name: DRY_RUN
              Value: 'false'
            - Name: INITIAL_START_DATE
              Value: !Ref InitialStartDate
            - Name: INITIAL_INGESTION_DATE
              Value: !Ref InitialIngestionDate
            - Name: BATCH_DAYS
              Value: '30'
          Secrets:
            - Name: BBALL_EMAIL
              ValueFrom: !Sub '${BballCredentialsSecret}:email::'
            - Name: BBALL_PSWRD
              ValueFrom: !Sub '${BballCredentialsSecret}:password::'
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: ecs
      # Additional permissions may be required for Glue catalog, CloudWatch logs, etc.

  DataUpdaterTaskExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      Path: /
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy

  ### ECS Task Definition ###
  DataUpdaterTask:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: nba-data-updater
      Cpu: '256'
      Memory: '512'
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      ExecutionRoleArn: !GetAtt DataUpdaterTaskExecutionRole.Arn
      TaskRoleArn: !GetAtt DataUpdaterTaskRole.Arn
      ContainerDefinitions:
        - Name: updater
          Image: !Ref DataUpdaterImage
          Essential: true
          Environment:
            - Name: BRONZE_BUCKET
              Value: !Ref BronzeBucketName
            - Name: BRONZE_PREFIX
              Value: bronze/nba
            - Name: AWS_REGION
              Value: !Ref AWS::Region
            - Name: DRY_RUN
              Value: 'false'
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: /ecs/nba-data-updater
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: ecs

  ### ECS Service ###
  DataUpdaterService:
    Type: AWS::ECS::Service
    Properties:
      Cluster: !Ref DataUpdaterCluster
      DesiredCount: 1
      LaunchType: FARGATE
      TaskDefinition: !Ref DataUpdaterTask
      NetworkConfiguration:
        AwsvpcConfiguration:
          Subnets: !Ref SubnetIds
          AssignPublicIp: ENABLED
      SchedulingStrategy: REPLICA

  ### ECS Cluster ###
  DataUpdaterCluster:
    Type: AWS::ECS::Cluster

  ### EventBridge Scheduler Rule ###
  DataUpdaterSchedule:
    Type: AWS::Scheduler::Schedule
    Properties:
      Name: nba-data-updater-schedule
      ScheduleExpression: cron(0 7 * * ? *)  # Daily at 07:00 UTC; adjust as needed
      FlexibleTimeWindow:
        Mode: FLEXIBLE
        MaximumWindowInMinutes: 30
      Target:
        Arn: !GetAtt DataUpdaterService.Arn
        RoleArn: !GetAtt SchedulerInvokeRole.Arn
        Input: '{}'

  SchedulerInvokeRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: scheduler.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: InvokeEcs
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: ecs:RunTask
                Resource: '*'
                Condition:
                  ArnEquals:
                    ecs:cluster: !Ref DataUpdaterCluster
              - Effect: Allow
                Action: iam:PassRole
                Resource: '*'

  ### Step Functions State Machine (Simplified) ###
  IngestionStateMachine:
    Type: AWS::StepFunctions::StateMachine
    Properties:
      RoleArn: !GetAtt StepFunctionsRole.Arn
      DefinitionString: |
        {
          "Comment": "Orchestrate NBA ingestion and transformations",
          "StartAt": "TriggerDataUpdater",
          "States": {
            "TriggerDataUpdater": {
              "Type": "Task",
              "Resource": "arn:aws:states:::ecs:runTask.sync",
              "Parameters": {
                "LaunchType": "FARGATE",
                "Cluster": "${DataUpdaterCluster}",
                "TaskDefinition": "${DataUpdaterTask}",
                "NetworkConfiguration": {
                  "AwsvpcConfiguration": {
                    "Subnets": ${SubnetIds},
                    "AssignPublicIp": "ENABLED"
                  }
                },
                "Overrides": {
                  "ContainerOverrides": [
                    {
                      "Name": "updater",
                      "Environment": [
                        { "Name": "DRY_RUN", "Value": "false" }
                      ]
                    }
                  ]
                }
              },
              "Next": "Success"
            },
            "Success": { "Type": "Succeed" }
          }
        }

  StepFunctionsRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: states.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: InvokeEcsFromStepFunctions
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - ecs:RunTask
                  - ecs:DescribeTasks
                Resource: '*'
              - Effect: Allow
                Action: iam:PassRole
                Resource: '*'